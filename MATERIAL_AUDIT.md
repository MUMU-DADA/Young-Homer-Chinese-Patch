# 汉化材料完整性审计

## 仓库边界

仓库只保存原创译文、构建脚本、开源字体和真正的二进制增量补丁。原版/汉化版 `data.win`、英文字符串表、反编译 GML、原始及派生游戏图片、字体导出、纹理图集和 UndertaleModTool 工作副本均由本地流程临时生成，并被 `.gitignore` 排除。

## 结论

当前版本需要汉化的玩家可见内容由 599 条运行时文本和 3 张烘焙英文图片组成。游戏没有原生语言表或语言选择入口，因此补丁覆盖默认英语。资源位于 `data.win` 内，不能用松散文件直接覆盖，发布采用动态写入方案。

原版 29 个字体资源均只有 96 个基础字形且不含中日韩文字；直接写入中文会显示方块。构建器会替换实际承载可翻译文本的 22 个字体资源，并按最终译文生成中文图集。其余字体没有承载汉化文本，不必扩大。

## 提取范围

- 完整导出 `data.win` 的 4266 条 STRG 字符串，而不是只搜显眼台词。
- 完整反编译可恢复的 560 个 GML 文件，用实际赋值和绘制上下文筛选玩家可见文本。
- 完整导出 78 个精灵资源（94 个子图 PNG）与 9 张嵌入纹理，逐项核对烘焙文字。
- 完整导出 29 个原字体，检查字形覆盖范围。
- 保留原始字符串编号、源文哈希、代码文件、行号和使用语句，供连续语境翻译与构建校验。

文本清单包括 38 个卡牌标题、38 个卡牌简介、三个等级各 71 段卡牌正文、152 段人物对白、18 段旁白、61 条观众评论、28 个诗题、40 条普通界面文本和 11 条控制提示。3 张图片是标题画面、开场提示和制作人员表；标题画面边缘卡牌上的部分可见英文也在范围内。

## 有意排除

- 引擎资源名、对象名、图层名、内部键名和不会展示给玩家的文件名。
- 输入库设备标识、调试信息、测试/模板资源和未实例化的参考精灵。
- 纯格式占位值，以及必须原样保留的按钮字母 `A/B/X` 和罗马数字 `I/II/III`。
- 制作人员姓名、团队名和字体名；图片中的栏目、职位和说明文字仍需翻译。

## 可重复验证

`tools/scripts/Refresh-Materials.ps1` 可从游戏根目录的原版 `data.win` 重新导出全部材料并重建翻译清单。`Build-ChinesePatch.ps1 -SmokeTest` 会在不启动游戏的前提下，将原文、测试中文字体和原图依次写入封包，再重新解析产物。正式构建还会拒绝空译文、错误占位符或换行、缺图、未改图片、尺寸变化、缺字以及错误游戏版本。

本审计只证明材料提取和制作链路完整，不替代用户约定的最终实机验收。
